<%= div_for line do %>
	<span>
		<%= link_to 'Edit', edit_line_path(line) %>
	  <%= link_to 'Destroy', line, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %>
	</span>
	<span><%= link_to "#{line.cue}", line %> : <%= "#{line.line}" %>
	</span>
<% end %>