<%= div_for scene do %>
	<span>
		<%= link_to 'Edit', edit_scene_path(scene) %>
	  <%= link_to 'Destroy', scene, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %>
	</span>
	<span><%= link_to "#{scene.name}", scene %></span>
<% end %>