<%= div_for script do %>
	<div class="pull-right">
		<%= link_to 'Edit', edit_script_path(script) %>
	  <%= link_to 'Destroy', script, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %>
	</div>
	<div><%= link_to "#{script.title}", script %></div>
<% end %>
